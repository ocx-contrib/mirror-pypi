# Generated by ocx-mirror v0.4.0 (rev unknown)
# DO NOT EDIT — re-run `ocx-mirror package pipeline generate ci --spec yt-dlp.mirror.yml` to update.
# Source: yt-dlp.mirror.yml

name: verify-yt-dlp

# Fail closed when a generated workflow is hand-edited: this job re-renders from
# yt-dlp.mirror.yml and diffs against what is committed. Re-run
# `ocx-mirror package pipeline generate ci --spec yt-dlp.mirror.yml` locally and commit the
# result to fix drift. To opt out (discouraged), set `allow_manual_edits: true` in
# yt-dlp.mirror.yml — the repository then owns its workflows by hand and this guard is not emitted.
on:
  pull_request:
    paths:
      - yt-dlp.mirror.yml
      - scripts/**
      - tests/**
      - metadata*.json
      - .github/workflows/**
  push:
    branches: [main]
    paths:
      - yt-dlp.mirror.yml
      - scripts/**
      - tests/**
      - metadata*.json
      - .github/workflows/**

permissions:
  contents: read

jobs:
  verify-generated:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - uses: ocx-sh/setup-ocx@8cd2f38eed07c9b79aaed22e61f9ba6b36e967a3  # v1.2.2
      - name: Check generated workflows are up to date
        # Exits 65 (DataError) on drift; the path-only hint names the stale file.
        run: |
          ocx-mirror package pipeline generate ci --check --spec yt-dlp.mirror.yml
