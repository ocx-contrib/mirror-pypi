# NOTICE

This repository packages and redistributes upstream Python packages from
[PyPI](https://pypi.org), resolved via [PEP 751](https://peps.python.org/pep-0751/)
`pylock.toml` files.

The Apache-2.0 license covers the OCX pipeline files authored here. It does
**not** cover upstream-derived assets — the Python packages published to
`dev.ocx.sh` carry their own upstream licenses (per-package, see each package's
PyPI project page).
